require = {deps: [], callback: (->)}
                     
# XXX: THESE SHOULD NOT BE PICKED UP                     
define ->
    require = {deps: [], callback: (->)}

require ->
    require = {deps: [], callback: (->)}
